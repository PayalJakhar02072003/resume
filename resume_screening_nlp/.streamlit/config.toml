# Do not set [server] port here.
# Streamlit Community Cloud always health-checks http://localhost:8501 — a custom
# port breaks deploy with "connection refused" on /healthz.
#
# Local machine: if 8501 is already taken, run:
#   streamlit run app.py --server.port 8502
